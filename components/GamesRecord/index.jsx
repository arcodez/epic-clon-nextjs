import React from "react";
import ListOfCards from "../ListOfCards";

const GamesRecord = () => {
  return <div>{<ListOfCards titleOfList="Juegos con logros >" />}</div>;
};

export default GamesRecord;
